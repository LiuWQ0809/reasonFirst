[project]
name = "chatgpt-selfhosted-gitlab-mcp"
version = "0.2.0"
description = "Read-only MCP server that lets ChatGPT inspect a self-managed GitLab through Secure MCP Tunnel"
requires-python = ">=3.10"
dependencies = [
  "mcp[cli]>=2,<3",
  "httpx>=0.27,<1",
]
